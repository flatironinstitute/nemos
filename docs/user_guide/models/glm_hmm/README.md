# GLM-HMM

:::{note}
Placeholder page. Planned content: the {class}`GLMHMM <nemos.glm_hmm.GLMHMM>` model (per-state GLM plus Markov dynamics), the EM fit, priors on the transition matrix and the emission weights, parameter initialization, and state decoding.
:::
