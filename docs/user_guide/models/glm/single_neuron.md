# Single Neuron GLM

:::{note}
Placeholder page. Planned content: {class}`GLM <nemos.glm.GLM>` end to end — configuring it, what `fit` does, reading `coef_` and `intercept_`, `predict` and `score`, and `simulate`.
:::
