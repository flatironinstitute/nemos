# Passing pynapple Objects to Bases and Models

:::{note}
Placeholder page. Planned content: the NeMoS entry points that accept pynapple objects and what they return — {meth}`Basis.compute_features <nemos.basis._basis.Basis.compute_features>`, including how a convolution treats epoch boundaries, and {meth}`fit <nemos.glm.GLM.fit>`, {meth}`predict <nemos.glm.GLM.predict>` and {meth}`simulate <nemos.glm.GLM.simulate>` — plus where the time axis stops being carried along. NeMoS does not require pynapple: every basis and model works on plain NumPy or JAX arrays.
:::
