# Solvers

:::{note}
Placeholder page. Planned content: how a regularizer constrains the choice of solver, the `"algo[backend]"` naming and the solver registry, the defaults NeMoS picks on your behalf (including Newton with a Cholesky solve for Ridge), and when to override them. Empirical comparisons live in the [benchmarks](solver-selection).
:::
