# Observation Models

:::{note}
Placeholder page. Planned content: the noise families ({class}`Poisson <nemos.observation_models.PoissonObservations>`, {class}`Gamma <nemos.observation_models.GammaObservations>`, {class}`Bernoulli <nemos.observation_models.BernoulliObservations>`, {class}`NegativeBinomial <nemos.observation_models.NegativeBinomialObservations>`, {class}`Gaussian <nemos.observation_models.GaussianObservations>`, {class}`Categorical <nemos.observation_models.CategoricalObservations>`), how to pick one for a given signal, inverse link functions, the estimated `scale_`, and what `score` returns.
:::
